// 將更新資料重新顯示 Form 上
$('.myform').html('<%= j render(partial: "form", locals: { menubar: @menubar, form_state: @form_state, current_menu_id: @current_menu_id } ) %>');
$('.toolbar').html('<%= j render(partial: "common/toolbar", locals: { menubar: @menubar, form_state: @form_state } ) %>');
